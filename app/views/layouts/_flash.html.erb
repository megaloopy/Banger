<% flash.each do |type, message| %>   <!-- Bootstrap flash. See Helpers/application_helper -->
  <div class="alert <%= bootstrap_class_for(type) %>" role="alert">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>